function repetir(n1, n2) {

    let resultado = []

    for (let i = 0; i < n2; i++)

    resultado.push(n1)

    return resultado
    }

    console.log (repetir (7,3))
    console.log (repetir("código", 2))